server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Proxy /api/* to the backend service, stripping the /api prefix.
  # proxy_buffering off is critical for SSE — without it nginx buffers the
  # stream and the client sees nothing until the connection closes.
  location /api/ {
    proxy_pass http://backend:8000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_http_version 1.1;
    proxy_set_header Connection '';
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 3600s;
  }

  # SPA fallback — all unmatched routes serve index.html so React Router works.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
